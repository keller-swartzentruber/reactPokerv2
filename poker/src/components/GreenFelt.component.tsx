import styled from "styled-components";

export const GreenFelt = () => {
  return <StyledDiv>Center Table Cards</StyledDiv>;
};

const StyledDiv = styled.div`
  height: 20rem;
  background: green;
`;
