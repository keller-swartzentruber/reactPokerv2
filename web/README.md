## Available Scripts

/// web
In the main project directory, run

### `npm run web`

/// api
In the main project directory, you can run:

### `npm run build`

then

### `npm run api`

to create python virtual env and listen

Open [http://localhost:3000](http://localhost:3000) to view it in the browser it id doesnt open automatically
